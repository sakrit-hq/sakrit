# Example: send_email with LangGraph

> **Superseded.** This early Act II placeholder has been replaced by real, tested examples:
>
> - **[LangGraph quickstart](../quickstarts/langgraph_quickstart.py)** — the same send-email graph,
>   crashed across an interrupt/resume, sending once instead of twice.
> - **[The golden money demo](../money_agent/)** — an agent that charges a card exactly once, even
>   through a crash in the dual-write window.
>
> Both are executed verbatim in CI, so the code you copy actually works.
